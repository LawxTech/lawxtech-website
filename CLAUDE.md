@AGENTS.md

# Pull requests

Always open PRs into `staging` first, never directly into `main`. Once the change is merged into `staging`, open a separate release PR from `staging` into `main`. Always in that order.
